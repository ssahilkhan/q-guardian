Yes! Server is running at http://localhost:8000
- Web Console UI: http://localhost:8000/ui/
- API Docs (Swagger): http://localhost:8000/docs
- Health Check: http://localhost:8000/api/v1/health
MongoDB connection failed (not running locally) but the app works fine — it reports degraded status for the DB dependency and continues serving.
Commands for future reference:
# Activate venv
.\venv\Scripts\Activate.ps1

# Run server
uvicorn src.q_guardian.main:app --host 0.0.0.0 --port 8000

# Or without activating venv
.\venv\Scripts\python.exe -m uvicorn src.q_guardian.main:app --host 0.0.0.0 --port 8000

# Run with auto-reload (dev mode)
uvicorn src.q_guardian.main:app --host 0.0.0.0 --port 8000 --reload
Open http://localhost:8000/ui/ in your browser to see the web console.