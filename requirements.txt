# ============================================================
# Q-Guardian Dependencies
# ============================================================
# Pinned runtime requirements for the API service (used by Docker).
#
# QA note (2026-08-21, release audit):
#   - Pins match the exact versions the 2,977-test suite and local
#     staging smoke test were validated against.
#   - python-jose / passlib / bcrypt / python-multipart were REMOVED:
#     zero imports anywhere in src/, tests, or scripts. This eliminates
#     all python-jose CVEs plus the unfixed transitive ecdsa CVE
#     (PYSEC-2026-1325). Re-add deliberately when auth is implemented.
#   - The authoritative dependency ranges live in pyproject.toml.
# ============================================================

# --- Core API ---
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.3

# --- Classical ML stack (required by the public analysis API) ---
numpy==2.5.1
scikit-learn==1.9.0
joblib==1.5.3

# --- Database ---
motor==3.7.1
pymongo==4.17.0

# --- Logging ---
structlog==26.1.0

# --- HTTP Client ---
httpx==0.28.1

# --- Utilities ---
orjson==3.12.0
python-dateutil==2.9.0.post0

# --- Production server (Docker image) ---
gunicorn==23.0.0

# --- Testing (dev/QA only; not required at runtime) ---
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
mongomock==4.3.0

# --- Code Quality (dev only) ---
ruff==0.16.4
mypy==2.3.1
pre-commit==4.6.2
